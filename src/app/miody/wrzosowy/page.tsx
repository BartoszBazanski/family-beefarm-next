import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Miód wrzosowy | Pasieka Szczygiełkowa',
};

export default function HeatherHoneyPage() {
  return (
    <section className="text-primary container min-h-screen pt-26 pb-10">
      ⚠️ To jest zastępczy tekst dla strony "Miód wrzosowy". Zostanie zaimplementowany w przyszłości. ⚠️
    </section>
  );
}
